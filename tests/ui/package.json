{
  "description": "Playwright runner framework for UI tests living in each integration's e2e/ directory.",
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/node": "^22.10.0",
    "typescript": "^5.6.3"
  },
  "name": "rasenmaeher-ui-tests",
  "private": true,
  "scripts": {
    "report": "playwright show-report",
    "screenshots": "SCREENSHOTS=1 playwright test",
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "test:ui": "playwright test --ui"
  },
  "type": "module",
  "version": "0.0.0"
}
